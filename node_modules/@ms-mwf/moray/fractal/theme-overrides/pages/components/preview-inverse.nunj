{% extends 'layouts/render.nunj' %}

{% set regExp = r/^preview.*/g %}
{% set entity = frctl.components.find('@' + request.params.handle) -%}
{% set withLayout = entity.preview + '-inverse' if not regExp.test(request.params.handle) else true -%}
{% set withCollation = true -%}

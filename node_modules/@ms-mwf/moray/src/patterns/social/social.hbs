<div class="row">
  <section class="col-12" role="region" aria-label="{{aria-label}}">
    {{ render '@headings' header}}
    {{#> '@lists'}}
      <a class="d-inline-block" aria-label="{{title}}" href="#" target="_blank">{{ render '@images' this }}</a>
    {{/ '@lists'}}
  </section>
</div>

{{#each filter}}
  {{#if accordion}}
    {{setProp accordion 'id' (uid)}}
    {{render '@accordion' accordion}}
  {{/if}}
  {{#if radio-group}}
    {{#if ratings}}{{render '@star-rating--filter' this merge=true}}
    {{else}}{{render '@radio-group' radio-group}}
    {{/if}}
  {{/if}}
{{/each}}

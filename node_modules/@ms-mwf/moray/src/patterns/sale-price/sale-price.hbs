{{#if sr-only}}<p class="sr-only">Originally starting from {{prev-price}} now starting from {{current-price}}</p>{{/if}}
<p{{#if sr-only}} aria-hidden="true"{{/if}} class="{{#if large-sale-price}}h3 font-weight-normal {{/if}}{{classes}}">
  {{~from~}}
  {{#if prev-price}} <span class="text-line-through text-muted">{{prev-price}}</span>{{/if}}
  <span class="font-weight-semibold">{{current-price}}</span>
</p>

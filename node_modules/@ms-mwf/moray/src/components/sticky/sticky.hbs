<div style="height:700px" class="container d-flex align-items-center">Scroll down...</div>

<div data-mount="sticky" data-direction="{{direction}}">
  {{#if body}}
    <div class="container">
      {{~#each body}}
        {{~#if paragraph}}{{> '@paragraphs--paragraph' paragraph}}{{/if~}}
      {{/each~}}
    </div>
  {{/if}}
</div>

<div style="height: 700px" class="container d-flex align-items-center">Scroll up...</div>

{{~#each templates}}
  {{~setProp this 'container-classes' (evaluate container-classes '||' 'mb-5')~}}
  <div class="{{#unless full-bleed}}container{{/unless}} {{container-classes}}">
    {{render (strConcat '@' name) data merge=true}}
  </div>
{{~/each}}
